# moveGradientAttsToTemplate

Moves gradient attributes to templates.

## Details

The plugin has no effect if:

- The document has scripts.

If a gradient element is only used as a template, any common attributes in the referencing gradients are moved to the template.
